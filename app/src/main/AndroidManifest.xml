<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.escoladeltreball.vogloclientm10">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UserActivity" />
        <activity android:name=".ServiceActivity" />
        <activity android:name=".BuscarUsuarios" />
        <activity android:name=".EliminarUsuario" />
        <activity android:name=".BuscarUsuarioPorId" />
        <activity android:name=".InsertarUsuario" />
        <activity android:name=".ActualizarUsuario"></activity>
    </application>

</manifest>